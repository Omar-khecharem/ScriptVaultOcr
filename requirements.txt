# ============================================================================
# ScriptVault OCR — Dépendances verrouillées (généré le 2026-08-01)
# ----------------------------------------------------------------------------
# Plateformes cibles : Windows x64, Linux x64, macOS arm64
# CPython supporté   : 3.9 → 3.13 (wheels vérifiés sur PyPI)
#
# Installation:  pip install -r requirements.txt
# Compilation:   pip install nuitka pyinstaller  (inclut dans ce fichier)
# ============================================================================

# --- Moteur OCR local HTR (TrOCR ONNX Runtime, remplace PaddleOCR) -----------
# Poids quantifiés int8 à déposer dans models/trocr/ via :
#   python -m scriptvault.download_trocr_models
onnxruntime>=1.20,<1.28     # ORT CPU — greedy decoding TrOCR (thread-safe)
opencv-contrib-python==4.10.0.84     # cv2.barcode + pipeline (>= 4.8 requis)
numpy==2.3.5                 # Arrays — compatible OpenCV 4.x

# --- Interface desktop (PySide6) --------------------------------------------
PySide6==6.11.1              # QThread + Signaux + Widgets (partie 2 et 3)
Pillow==12.3.0               # Images (TIFF multipages, aperçus)

# --- Exportations ------------------------------------------------------------
reportlab==5.0.0             # PDF avec couche texte sélectionnable
python-docx==1.2.0           # Export Word .docx
PyMuPDF==1.28.0              # Rasterisation PDF page par page

# --- Compilation / Packaging (build uniquement) -----------------------------
nuitka==4.1.3                # Compilateur C++ (recommandé, source cachée)
pyinstaller==6.21.0          # Alternative (bundleur Python)
