# ============================================================================
# ScriptVault OCR — Desktop (application PySide6)
# ----------------------------------------------------------------------------
# Dépend du package partagé `scriptvault` (moteur OCR + exports), installé
# depuis ../backend. Un seul environnement (venv) pour backend + desktop.
#   pip install -r requirements.txt
# Lancement :  python gui_app.py [--lang fr]
# Build exe :  python build_installer.py --tool nuitka --mode onefile
# ============================================================================

-e ../backend                          # moteur OCR partagé (paddle, API, exports)

# --- Interface desktop -------------------------------------------------------
PySide6==6.11.1                        # QThread + Signaux + Widgets
Pillow==12.3.0                         # Images (dépendance PySide6/paddleocr)

# --- Compilation / Packaging (build uniquement) ------------------------------
nuitka==4.1.3                          # Compilateur C++ (recommandé, source cachée)
pyinstaller==6.21.0                    # Alternative (bundleur Python)
zstandard>=0.22                        # Recommandé par Nuitka (compression)
ordered-set>=4.1                       # Recommandé par Nuitka
